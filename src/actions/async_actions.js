import * as caller from "../middleware/api/caller"
import * as actions_types from '../constants/action-types'


/**
 * Keep all asynchronize Redux Action Declaration Here
 * Can also make actions/async folder if file becomes huge
 */