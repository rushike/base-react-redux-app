import * as caller from "../middleware/api/caller"
import * as actions_types from '../constants/action-types'

/**
 * Keep all synchronize Redux Action Declaration Here
 * Can also make actions/sync folder if file becomes huge
 */